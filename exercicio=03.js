const x = 10, y = 2;

let resultado = x + y;

let mensagem = "A soma de x + y = "

console.log(`${mensagem}${resultado}`);

resultado = x - y;
mensagem = "A diminuição de x - y = ";

console.log(`${mensagem}${resultado}`);

resultado = x * y;
mensagem = "A multiplicação de x * y = ";

console.log(`${mensagem}${resultado}`);

resultado = x / y;
mensagem = "A divisão de x / y = ";

console.log(`${mensagem}${resultado}`);